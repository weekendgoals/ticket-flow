**Issues found.**

**Blocking — confirmed. RUN-3's only CHECK cannot pass on any tree.** The plan: `CHECK: grep -c '^- \*\*' plugins/flow/skills/retro/SKILL.md` / `EXPECT: 7`. On the current tree that command prints **9** — step 4 has six bold bullets (`retro/SKILL.md:66-89`) and step 6 has three more (`:116`, `:122`, `:130`). After the seventh question it prints 10, never 7. This is FND-6's class exactly (a CHECK that fails on the correct tree; the driver would halt the ticket at the acceptance gate). Scope it to the step-4 region — e.g. `awk '/^## 4\./,/^## 5\./' plugins/flow/skills/retro/SKILL.md | grep -c '^- \*\*'` — which prints 6 now and 7 after, the shape RUN-2 asks for.

**Blocking — confirmed. RUN-4's advertised recovery does not work in the state that needs it.** The plan: "after a halt, the way to continue is to **re-run** `/flow:run <epic>` — the board skips integrated tickets, so a re-run resumes after them — and never `resumeFromRunId`". But the run skill refuses to start when "any ticket is `in-progress`, `in-review` or `done`" (`skills/run/SKILL.md:38-47`), and a ticket halted mid-pass — branch pushed, DONE entry, addendum committed, unmerged — derives as `done` (`scripts/tickets.mjs:620-624`). Ten of the twelve cited halts were mid-ticket (fix-bounds ×3, acceptance ×2, re-review Important, reviewer spawn ×2, budget excepted). So "never resume" strands the common case unless the ticket also says what the human does first: close the ticket by hand (`/flow:ticket <ID>` step 10, or the hand merge that GHL-1/3/9/10 actually got), then re-run. The CLAUDE.md invariant is explicit: a refusal's advertised recovery must work in the refused state. Two further problems in the same ticket, plausible: (a) the driver half's mechanism does not match its evidence — the cited replay ("first resume replayed the stale failed acceptance from cache") followed a *plugin* fix with the branch unchanged at `8ca4b15a`, so a prompt pinned to the head SHA would have replayed identically; the branch-changed case the SHA actually protects against is GHF-2's reconcile resume (`a5d96449`), which the plan does not cite. (b) The runtime's cache is a *prefix* cache — "the longest unchanged prefix of agent() calls returns cached results; the first edited call and everything after it runs live" (workflow-authoring reference) — so a SHA in the tier-facts prompt means any resume after a pushed fix re-runs review and disposition live, writing a second addendum on a branch that already carries one. And the worker reports no head SHA today (`WORKER_SCHEMA`, `run-epic.mjs:263-296`; the Codex runner's JSON at `scripts/runners/codex.mjs:98-116` neither), so "the worker's report for tier-facts" adds a schema field to both plus the runner's test — files RUN-4's scope does not name. If the skill rule is the fix, say whether the driver half survives at all.

**Blocking — confirmed. RUN-2's second doctor shape is inverted against its own evidence.** The plan: "a `grep -c`/`grep -rc` CHECK whose EXPECT is a bare number while the command names a path". FND-6's actual line had **no EXPECT** — `node -e "...execSync('grep -rc \"…\\|…\" src/functional/structured-data.ts').toString().trim();process.exit(n==='1'?0:1)"` (weekendgoals `redesign-foundation/tickets.md` history) — so the rule misses the case it cites. Meanwhile `grep -c "Me and the log" api-gateway/CLAUDE.md` / `EXPECT: 1` (GHF-1, passed 2/2 live) names one path with `-c` and prints a bare count — verified here with both BSD grep 2.6 and ugrep: single file, `-c` → `1`; `-rc` → `path:1`. As written the rule flags the sound CHECK and passes the defective one, contradicting the AC "says nothing about a sound CHECK". Restate it as: `-r` combined with `-c` (any spelling) in a CHECK line, whatever the comparison. Same bullet, shape one: both original `\\|` lines have since been repaired in the weekendgoals documents (FND-2 now `grep -rEn 'MobileSidebar|mobile-sidebar'`, FND-6 `grep -Ec`), so the fixture must be quoted from history — the shape is `\\|` inside a `node -e` string (three instances: FND-2, FND-4's `£` check, FND-6), not a bare `\|`, which GNU grep would accept as alternation.

**Nit.** RUN-1's evidence sentences are wrong in two places that will land in METHODOLOGY and the CHANGELOG: "three hand re-reviews at ~40k tokens each" — GHL-1 got none ("merged by hand… Vadim's standing rule since is that translation keys never need a second review", `groundhopper-log/status.md`); the two re-reviews were GHL-9 (41,368) and GHL-10 (40,465). And "five of groundhopper-foundation's eight CHECKs" carry `EXPECT: Tests:` — it is six of eight (`groundhopper-foundation/tickets.md:257,362,424,470,539,606`). Also unstated: PR #42's `Fix bounds exclude:` exemption **never reached main** — `8ac3bef` is contained only by `origin/addendum-gate-date`, and `run-epic.mjs` has no exclusion beyond `epics/` (`:1319-1320`). RUN-1 supersedes it; say so, or a builder (and the user's own memory rule) will look for a line that does not exist.

**Nit.** The ground rule "`scripts/check-invariants.mjs` holds the coupling" of STOP strings is untrue: PHRASES holds only `a failed acceptance CHECK` and `toward the default branch` (`check-invariants.mjs:255-306`). RUN-1's "The stop-condition coupling holds" CHECK is therefore decorative for `STOP.fixBounds`; add that sentence to PHRASES in RUN-1. And the below-consequence doctrine has more copies than RUN-1 names: `run/SKILL.md:191-196`, `:293-296`, `:351`, `:410`, `README.md:18`, `meta.phases` at `run-epic.mjs:12`, the schema comment at `:346-355` — the "one rule in many documents" ground rule applies to RUN-1 itself.

**Nit.** Eight of the plan's nine CHECKs pass on the pre-ticket tree (run-epic suite `# pass 91`, tickets suite `# pass 56`, the invariants and parse checks) and the ninth can never pass — the plan fails the rule RUN-2 introduces. `EXPECT: # pass <n+3>` or `--test-name-pattern` on the new cases would fail before and pass after; note CLAUDE.md's counts (78, 36) are stale against the working tree, so any numeric EXPECT must be read from the branch point.

**Nit.** RUN-1's third bullet (finding-named files join `reviewedFiles`) has no test in the AC, which names exactly three cases. And halting on `STOP.fixBounds` when the bounds re-review finds an Important gives one event two stop strings — the existing re-review halts on `STOP.importantFinding` (`run-epic.mjs:1217-1226`, `run/SKILL.md:288-292`) — which RUN-3's classifier would read as two classes.

**Nit.** RUN-2 has the plan reviewer run every CHECK, while keeping doctor read-only because "a CHECK may have side effects". The same epic's own preamble records Cypress CHECK lines that "opened the interactive runner and started no server" (`redesign-foundation/tickets.md:13`), and a jest IT CHECK prints ~1.2 MB over minutes. Give the reviewer a timeout and an opt-out, or have the planner run them and hand over the ledger.

**Questions for sign-off.**

1. RUN-2 demotes whole-suite CHECKs from the driver's code merge gate to the worker's Verified line — an agent claim. METHODOLOGY §"Why acceptance criteria can be machine-runnable" (`:276-311`) says CHECK exists to move exactly that claim to repository state. Accept the trade, or keep suite CHECKs as gates and require in addition one CHECK per ticket that fails pre-ticket?
2. PR #42 (`Fix bounds exclude:`) never merged. Does RUN-1 retire it, or does it land too? (Translation fan-outs would otherwise buy an opus re-review each time.)
3. Three of the twelve halts were session limits, which no ticket can touch; the Outcome's "<0.25 halts per ticket" can fail on those alone. Exclude environment halts from the figure, or accept the metric as is?
4. The status log's two-writers-one-tail conflict (retro-foundation Halt 2c: run records and ticket entries both append to the same file end, so every mid-ticket halt followed by a hand merge conflicts) is plugin-caused and unowned here. In this epic, or deferred with a named owner?
5. RUN-4: with "never resume" as doctrine, does the driver half survive; if so, for which resume case, and what should a resume after a pushed fix do given the prefix cache re-runs review and disposition?

**Checked and sound.** RUN-1's decomposition matches the code: the branch it replaces is exactly `run-epic.mjs:1387-1413` with `FIX_LINE_BUDGET` at `:139`, `hireReviewer` (`:581-603`) and `RE_REVIEW_SCHEMA` are reusable as-is, and the test harness already stubs `re-review:<ID>` and resolve FACT 3 (`run-epic.test.mjs:570-711`), so the three named cases are writable. The retro miner's §7 recommendation ("replace the fix-bounds halt with the automatic consequence-tier re-review; do not raise the budget") independently supports RUN-1 and its Not-in-scope. The Outcome is falsifiable with a named observer and reversal; the order de-risks (the one assumption is proven by RUN-1's Important-halts case); incremental delivery is right for a repo whose defects halt unattended runs; no two tickets collide on a file within one pass.